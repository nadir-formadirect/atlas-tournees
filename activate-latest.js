(()=>{'use strict';try{const all=JSON.parse(localStorage.getItem('atlas_datasets_v2')||'[]');const id=localStorage.getItem('atlas_active_dataset_v2');const active=all.find(x=>x.id===id);if(active&&Array.isArray(active.routes)&&active.routes.length){window.ROUTES=active.routes;window.ACTIVE_DATASET=active;return}}catch{}if(Array.isArray(window.DAILY_ROUTES)&&window.DAILY_ROUTES.length){window.ROUTES=window.DAILY_ROUTES;window.ACTIVE_DATASET={label:'Plan quotidien intégré',date:window.DAILY_ROUTES.find(r=>r.date)?.date||'',source:'application'};}})();